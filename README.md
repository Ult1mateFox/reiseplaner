Benutzer gibt Daten ins Formular ein.
React speichert diese Daten in formData.
Beim Klick auf den Button wird generatePackingList(formData) aufgerufen.
Das Ergebnis wird in packingList gespeichert.
Die Liste wird unten angezeigt.
